defmodule Walybot.Command.AddTranslator do
  def process(text, update) do
    # TODO parse the command
    # if good? => add to the database, send back ack to the user
    # of bad? => send error message back to user
    :ok
  end
end
